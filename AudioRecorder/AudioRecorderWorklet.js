class AudioRecorderProcessor extends AudioWorkletProcessor{constructor(){super(),this.bufferSize=2048,this.sampleBuffer=new Float32Array(this.bufferSize),this.sampleOffset=0,this.port.onmessage=event=>{"flush"===event.data?.type&&(this.flushSamples(),this.port.postMessage({type:"flushed"}))}}flushSamples(){if(this.sampleOffset<=0)return;const samples=this.sampleBuffer.slice(0,this.sampleOffset);this.sampleOffset=0,this.port.postMessage({type:"samples",samples:samples},[samples.buffer])}pushSamples(input){let readOffset=0;for(;readOffset<input.length;){const available=this.bufferSize-this.sampleOffset,count=Math.min(available,input.length-readOffset);this.sampleBuffer.set(input.subarray(readOffset,readOffset+count),this.sampleOffset),this.sampleOffset+=count,readOffset+=count,this.sampleOffset>=this.bufferSize&&this.flushSamples()}}process(inputs,outputs){const input=inputs[0]?.[0];input&&this.pushSamples(input);const output=outputs[0]?.[0];return output&&output.fill(0),!0}}registerProcessor("audio-recorder-processor",AudioRecorderProcessor);